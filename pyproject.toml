[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lsso-operator"
version = "0.1.1"
description = "Learnable Sylvester Solve Operator token mixer for encoder models."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
    { name = "Zhaoyang Wang", email = "zhaoy@uir.edu.cn" },
]
keywords = [
    "pytorch",
    "token-mixer",
    "sylvester",
    "efficient-transformer",
    "retrieval",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "torch>=2.1",
]

[project.urls]
Repository = "https://github.com/Yang916-yy/LSSO"
Paper = "https://github.com/Yang916-yy/LSSO/blob/main/paper/LSSO_Learnable_Low-Rank_Sylvester_Solves_for_Efficient_Bidirectional_Encoders_v1.pdf"
Issues = "https://github.com/Yang916-yy/LSSO/issues"

[project.optional-dependencies]
triton = [
    "triton",
]
hf = [
    "transformers",
    "datasets",
    "accelerate",
    "evaluate",
    "scikit-learn",
]
experiments = [
    "matplotlib",
    "numpy",
    "tqdm",
    "torchinfo",
    "transformers",
    "datasets",
    "accelerate",
    "evaluate",
    "scikit-learn",
]
all = [
    "matplotlib",
    "numpy",
    "tqdm",
    "torchinfo",
    "transformers",
    "datasets",
    "accelerate",
    "evaluate",
    "scikit-learn",
]

[tool.setuptools.packages.find]
include = ["lsso*"]
exclude = ["data*", "runs*", "runs_archive*", "scripts*", "tests*", "notebooks*"]

[tool.setuptools]
include-package-data = false

[tool.setuptools.package-data]
lsso = ["py.typed"]
